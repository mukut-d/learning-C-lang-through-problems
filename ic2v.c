#include<stdio.h>
#include<math.h>
int main()
{ int x,n,r;
printf("enter the value of x and n");
 scanf("%d %d",&x, &n);
r = (1+ 2 *x);
 int res = pow(r,n);
 printf("the resultant is %d",res);
return 0;
}

